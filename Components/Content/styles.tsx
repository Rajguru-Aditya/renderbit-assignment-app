import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    gap: 20,
  },

  text: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#000',
  },
})

export default styles